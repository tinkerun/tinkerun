const quickConnection = {
  id: 'quick_connection',
  name: 'Quick Connection',
}

const appName = 'Tinkerun'

const protocol = 'tinkerun'

module.exports = {
  quickConnection,
  appName,
  protocol,
}
